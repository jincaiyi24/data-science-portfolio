# EV SQL 项目完整学习手册

> 本手册以项目中的真实文件、真实预测和真实指标为准；没有交易数据，因此不推导 GMV、ROI、CAC 或 LTV。

## 第 1 章 项目问题与数据边界

**概念是什么：** 把购买意向排序、客户画像和模型验证放进同一分析链路，同时承认数据为合成数据、目标是意向而非成交。

**为什么本项目需要：** 先界定证据边界，才能避免把相关性说成因果。

**代码位置：** `README.md；reports/project_audit.md`

**代码示例：**

```bash
python scripts/run_mysql_pipeline.py
```

**输入：** 原始竞赛数据、OOF 预测、测试集预测

**输出：** 可验证的问题定义和限制说明

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“项目问题与数据边界”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 2 章 MySQL 在本项目中的角色

**概念是什么：** MySQL 负责关系建模、质量门禁、可复用查询和业务结果导出，不负责重新训练模型。

**为什么本项目需要：** 让建模结果能被分析师、BI 和面试官独立复核。

**代码位置：** `scripts/run_mysql_pipeline.py`

**代码示例：**

```bash
python scripts/run_mysql_pipeline.py
```

**输入：** Python 已产出的特征、预测、分群和指标

**输出：** 数据库表、查询结果与对账记录

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“MySQL 在本项目中的角色”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 3 章 为什么采用四表模型

**概念是什么：** 特征、预测、分群、实验指标具有不同粒度和更新节奏，拆表后责任清楚。

**为什么本项目需要：** 避免一张超宽表混合事实、派生标签和实验元数据。

**代码位置：** `sql/01_schema.sql`

**代码示例：**

```sql
-- Executable reference: sql/01_schema.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 四类实体

**输出：** 四张规范化表及外键

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“为什么采用四表模型”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 4 章 主键设计

**概念是什么：** 三个客户级表都使用全局唯一 id；指标表使用自增 metric_id。

**为什么本项目需要：** 数据核验确认 train/test id 区间不重叠，因此 id 可单独做主键。

**代码位置：** `sql/01_schema.sql`

**代码示例：**

```sql
-- Executable reference: sql/01_schema.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 训练集 id 0-668664；测试集 id 668665-955235

**输出：** 稳定的一对一连接键

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“主键设计”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 5 章 外键与参照完整性

**概念是什么：** 预测和分群必须先有客户特征记录，删除客户时级联清理派生记录。

**为什么本项目需要：** 把漏预测、孤立分群从静默错误变成装载失败。

**代码位置：** `sql/01_schema.sql`

**代码示例：**

```sql
-- Executable reference: sql/01_schema.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 客户、预测和分群 id

**输出：** 两条外键约束

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“外键与参照完整性”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 6 章 索引设计

**概念是什么：** 为 source_set、city、subsidy、segment、decile 和 probability 建立查询导向索引。

**为什么本项目需要：** 索引服务真实查询，不为展示语法而堆砌。

**代码位置：** `docs/sql/index_design.md`

**代码示例：**

```sql
-- Executable reference: docs/sql/index_design.md
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 常用 WHERE、JOIN、GROUP BY 和 ORDER BY 列

**输出：** 二级索引与 EXPLAIN 报告

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“索引设计”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 7 章 环境变量与连接安全

**概念是什么：** 连接参数来自 .env 或 DATABASE_URL，日志只显示掩码地址。

**为什么本项目需要：** 避免密码进入代码、README、日志和 Git 历史。

**代码位置：** `src/mysql/connection.py；.env.example`

**代码示例：**

```sql
-- Executable reference: src/mysql/connection.py；.env.example
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** host、port、user、password、database

**输出：** SQLAlchemy Engine

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“环境变量与连接安全”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 8 章 数据库创建与幂等性

**概念是什么：** 数据库和表使用 IF NOT EXISTS；数据刷新只清空本项目四表。

**为什么本项目需要：** 一键脚本失败后可以修复配置再重跑。

**代码位置：** `src/mysql/connection.py；src/mysql/schema.py`

**代码示例：**

```sql
-- Executable reference: src/mysql/connection.py；src/mysql/schema.py
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 数据库名和 DDL

**输出：** 可重复运行的结构

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“数据库创建与幂等性”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 9 章 训练集与测试集合并

**概念是什么：** 统一字段后增加 source_set，测试集目标保留 NULL。

**为什么本项目需要：** 既可全量画像，又不会把未知测试标签伪装成 No。

**代码位置：** `src/mysql/loader.py::_prepare_features`

**代码示例：**

```sql
-- Executable reference: src/mysql/loader.py::_prepare_features
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** train.csv、test.csv

**输出：** 955236 行客户特征

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“训练集与测试集合并”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 10 章 真实 OOF 与测试预测

**概念是什么：** 训练行使用 advanced blend OOF，测试行使用对应真实 test prediction。

**为什么本项目需要：** 训练行不能使用全量拟合后的 in-sample 预测，否则验证分析会泄漏。

**代码位置：** `src/mysql/loader.py::_prepare_predictions`

**代码示例：**

```sql
-- Executable reference: src/mysql/loader.py::_prepare_predictions
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** advanced_blend_oof/test_predictions.csv

**输出：** 955236 行概率

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“真实 OOF 与测试预测”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 11 章 预测十分位和百分位

**概念是什么：** 在 train/test 各自内部排名，1 表示最高十分位，百分位越高代表倾向越高。

**为什么本项目需要：** 分别排名避免两个集合样本量不同造成分箱偏移。

**代码位置：** `src/mysql/loader.py::_rank_predictions`

**代码示例：**

```sql
-- Executable reference: src/mysql/loader.py::_rank_predictions
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** predicted_probability

**输出：** prediction_decile、prediction_percentile

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“预测十分位和百分位”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 12 章 既有客户分群复用

**概念是什么：** 分群沿用多随机种子 XGBoost 业务模型及原规则，不用最终竞赛特征重新定义。

**为什么本项目需要：** 业务可解释性和比赛最优分数是两个目标，不能事后偷换口径。

**代码位置：** `src/business_analysis.py；src/mysql/loader.py::_prepare_segments`

**代码示例：**

```sql
-- Executable reference: src/business_analysis.py；src/mysql/loader.py::_prepare_segments
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** dashboard 行级分群结果

**输出：** 六类 customer_segment

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“既有客户分群复用”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 13 章 模型指标入库

**概念是什么：** 汇总模型比较、逐折结果、多种子稳定性、特征消融和融合实验。

**为什么本项目需要：** NULL 表示原实验未产出该指标，不补造数字。

**代码位置：** `src/mysql/loader.py::_prepare_metrics`

**代码示例：**

```sql
-- Executable reference: src/mysql/loader.py::_prepare_metrics
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** outputs/tables/*.csv

**输出：** 55 条真实实验记录

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“模型指标入库”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 14 章 分块装载

**概念是什么：** pandas.to_sql 按块写入，失败时缩小块大小并最多重试三次。

**为什么本项目需要：** 接近三百万行写入时，分块能控制内存与单次报文大小。

**代码位置：** `src/mysql/loader.py::_load_frame`

**代码示例：**

```sql
-- Executable reference: src/mysql/loader.py::_load_frame
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 四张 DataFrame

**输出：** 装载日志和数据库记录

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“分块装载”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 15 章 事务与安全刷新

**概念是什么：** 在单连接中暂时关闭外键检查，按子表到父表顺序 TRUNCATE，再恢复约束。

**为什么本项目需要：** 全量快照比逐行 upsert 更简单；生产增量场景应改用 staging + merge。

**代码位置：** `src/mysql/loader.py::load_tables`

**代码示例：**

```sql
-- Executable reference: src/mysql/loader.py::load_tables
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 已存在的本项目表

**输出：** 干净的全量快照

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“事务与安全刷新”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 16 章 数据质量门禁

**概念是什么：** 核验行数、唯一性、目标缺失规则、概率范围与连接覆盖率。

**为什么本项目需要：** 把可复现性从一句承诺变成机器可判定的 PASS/FAIL。

**代码位置：** `src/mysql/validator.py；sql/02_data_quality.sql`

**代码示例：**

```sql
-- Executable reference: src/mysql/validator.py；sql/02_data_quality.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 数据库四表和本地预期行数

**输出：** mysql_validation.csv/report.md

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“数据质量门禁”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 17 章 客户画像 SQL

**概念是什么：** CASE WHEN 生成年龄和收入分层，GROUP BY 比较城市画像。

**为什么本项目需要：** 只在有真实标签的训练集计算 observed purchase rate。

**代码位置：** `sql/03_customer_profile.sql`

**代码示例：**

```sql
-- Executable reference: sql/03_customer_profile.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 训练集客户特征

**输出：** 画像组合的规模、购买率和均值

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“客户画像 SQL”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 18 章 倾向分析 SQL

**概念是什么：** CTE 先完成三表连接，再按城市、补贴、充电和焦虑维度聚合。

**为什么本项目需要：** CTE 让派生数据集只定义一次，主查询更易审查。

**代码位置：** `sql/04_purchase_propensity.sql`

**代码示例：**

```sql
-- Executable reference: sql/04_purchase_propensity.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 特征与最终模型预测

**输出：** 分组客户量和平均倾向

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“倾向分析 SQL”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 19 章 连接覆盖率

**概念是什么：** LEFT JOIN 保留全部客户，统计预测或分群缺口。

**为什么本项目需要：** INNER JOIN 会吞掉缺失行，无法发现覆盖问题。

**代码位置：** `sql/06_join_analysis.sql`

**代码示例：**

```sql
-- Executable reference: sql/06_join_analysis.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 三张客户级表

**输出：** train/test 连接率

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“连接覆盖率”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 20 章 窗口函数

**概念是什么：** 使用 SUM OVER、DENSE_RANK、ROW_NUMBER 和 LAG 做组内占比、排序与差距。

**为什么本项目需要：** 窗口函数保留明细层级，普通 GROUP BY 做不到。

**代码位置：** `sql/07_window_functions.sql`

**代码示例：**

```sql
-- Executable reference: sql/07_window_functions.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 城市、收入层和分群聚合

**输出：** 组内排名和前后差

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“窗口函数”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 21 章 模型与实际结果比较

**概念是什么：** 在训练集十分位上比较平均预测与实际正例率，观察排序和校准差。

**为什么本项目需要：** ROC-AUC 衡量排序而非概率校准，两者需要分开检查。

**代码位置：** `sql/09_model_validation.sql`

**代码示例：**

```sql
-- Executable reference: sql/09_model_validation.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 真实 OOF 概率和 actual_target

**输出：** 十分位校准表

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“模型与实际结果比较”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 22 章 业务 KPI 边界

**概念是什么：** 只计算客户数、意向率、模型倾向、充电和补贴等数据支持的 KPI。

**为什么本项目需要：** 没有交易、成本和营收数据，因此不虚构 GMV、CAC、LTV 或 ROI。

**代码位置：** `sql/10_business_kpi.sql`

**代码示例：**

```sql
-- Executable reference: sql/10_business_kpi.sql
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 四表可验证字段

**输出：** 一行核心 KPI

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“业务 KPI 边界”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 23 章 Python 与 SQL 对账

**概念是什么：** 同一组指标分别由 pandas 和 MySQL 计算，差异超过 1e-10 即失败。

**为什么本项目需要：** 对账能发现分母、NULL 处理和过滤口径的不一致。

**代码位置：** `src/mysql/exporter.py`

**代码示例：**

```sql
-- Executable reference: src/mysql/exporter.py
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 内存表与数据库表

**输出：** 13_python_sql_reconciliation.csv

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“Python 与 SQL 对账”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 24 章 查询性能分析

**概念是什么：** 对代表性聚合和窗口查询执行 EXPLAIN，结合访问类型、key 和 rows 判断。

**为什么本项目需要：** 低基数字段即使有索引，全表聚合仍可能选择扫描，这是成本模型的正常决策。

**代码位置：** `src/mysql/query_runner.py::write_performance_report`

**代码示例：**

```sql
-- Executable reference: src/mysql/query_runner.py::write_performance_report
SELECT COUNT(*) AS checked_rows FROM ev_customer_features;
```

**输入：** 三条核心 SQL

**输出：** query_performance_report.md

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“查询性能分析”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。

## 第 25 章 复现、发布与审计

**概念是什么：** 一键脚本、输出清单、敏感信息扫描和公开包边界共同构成可交付证据。

**为什么本项目需要：** 公开仓库排除原始数据、行级客户结果、预测、模型和凭据。

**代码位置：** `scripts/run_mysql_pipeline.py；tools/build_github_safe_package.ps1`

**代码示例：**

```bash
python scripts/run_mysql_pipeline.py
```

**输入：** 代码、配置模板和本地数据

**输出：** 可运行项目与安全公开包

**先后逻辑：** 先确认输入口径和唯一键，再执行转换或查询，最后用行数、范围或连接覆盖进行验证。

**为什么这样写：** 选择最直接且可审计的实现，让代码、SQL 和导出结果能够互相追溯。

**替代方案：** 小数据可完全用 pandas；生产大数据可使用 staging、批量加载、调度器和数据仓库。替代方案是否更好取决于并发、延迟和治理要求。

**常见错误：** 混淆训练/测试口径、把 NULL 当 0、用 INNER JOIN 掩盖缺口、把相关性当因果，或在没有原始证据时补造指标。

**面试回答：** 我在本项目中用这一设计解决了“复现、发布与审计”问题，并通过自动验证与导出结果证明实现可复现。

**高级追问：** 如果数据量扩大十倍或改为每日增量，当前方案哪里先成为瓶颈？

**参考答案：** 全量 CSV 读取和 TRUNCATE 重载会先受到 I/O 与恢复时间限制；应改成 staging 表、增量水位、批量装载和原子换表，并保留同样的质量门禁。
